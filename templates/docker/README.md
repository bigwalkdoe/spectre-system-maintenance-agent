# Template: Docker

Docker and Docker Compose configuration templates.

## Contents

- `Dockerfile` — multi-stage Python service
- `Dockerfile.node` — multi-stage Node.js service
- `docker-compose.yml` — service + postgres + redis
- `.dockerignore`
