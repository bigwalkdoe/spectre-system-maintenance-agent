# Next.js Patterns

- App Router (`app/` directory) — not Pages Router
- Server Components by default, Client Components when needed (`"use client"`)
- Route handlers in `app/api/*/route.ts`
- Data fetching in Server Components (async/await)
- Tailwind CSS for styling
- Environment variables via `NEXT_PUBLIC_*` prefix for client-side
