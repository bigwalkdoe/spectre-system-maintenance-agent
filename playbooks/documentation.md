# Update Documentation

## Steps

1. Identify what changed — new features, changed APIs, removed functionality
2. Update `README.md` if the change affects the overall project
3. Update API endpoint docs if routes changed
4. Update architecture docs if system design changed
5. Update inline docstrings (if any — prefer none)
6. Update environment variable docs if config changed
7. Run `mkdocs serve` or equivalent to preview
